# Taps
tap "hashicorp/tap"

# === Formulae (CLI) ===
brew "gh"                  # GitHub CLI
brew "nvm"                 # Node version manager (sourced by zsh/conf.d/20-tools.zsh)
brew "corepack"            # pnpm/yarn shim
brew "direnv"              # env loader (hook in zsh/conf.d/20-tools.zsh)
brew "libpq"               # PATH wired in zsh/conf.d/10-path.zsh
brew "tmux"
brew "gnupg"
brew "python@3.14"
brew "hashicorp/tap/terraform"

# === Casks (GUI) ===
cask "iterm2"
cask "google-chrome"
cask "tailscale-app"             # cask was renamed from "tailscale" upstream
cask "font-meslo-lg-nerd-font"   # font for powerlevel10k
